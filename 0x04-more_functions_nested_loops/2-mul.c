#include "main.h"
/**
 * mul - function prints a multiplication between integer
 * @a: first integer
 * @b: second integer
 * Return: a*b (multiplication)
 */
int mul(int a, int b)
{
	return (a * b);
}
